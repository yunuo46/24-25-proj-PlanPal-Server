spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  ai:
    vertex:
      ai:
        gemini:
          project-id: "gen-lang-client-0798322573"
          location: "asia-northeast3"
          chat:
            options:
              model: gemini-2.0-flash
  config:
    import: classpath:secret.yml

